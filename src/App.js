import React,{useEffect,useMemo,useRef,useState} from 'react';
import * as THREE from 'three';
import {AnimatePresence,motion} from 'framer-motion';
import {ArrowUpRight,Code2,Github,Linkedin,Mail,Moon,Rocket,Sparkles,Sun,Terminal,X} from 'lucide-react';
import './App.css';

const projects=[['01','Knowledge Base','LOCAL AI / RAG','Codebase-aware retrieval with vector search, BM25, AST chunking and persistent developer memory.',['Python','FAISS','SQLite','Tree-sitter']],['02','SoulOS','MULTI-LLM / DESKTOP','Desktop AI orchestration with model routing, tools, persistent sessions and 200+ categorized actions.',['Python','PyQt5','Flask','OpenRouter']],['03','Stealth Assistant','MULTIMODAL AI','Background assistant combining live transcription, OCR screen understanding, VAD and WebSockets.',['Whisper','OCR','WebSockets']],['04','SlideNova','GEN-AI','Natural-language prompts into structured PowerPoint presentations with themes and automated generation.',['Python','Gemini','PyQt6']]];
const skills=['Python','FastAPI','Flask','React','LLMs','RAG','LangGraph','PyTorch','OpenCV','PostgreSQL','Supabase','Docker'];
const phase=h=>h>=5&&h<8?'dawn':h>=8&&h<17?'day':h>=17&&h<20?'golden':'night';

function ParticleBackground(){const ref=useRef(null);useEffect(()=>{const host=ref.current;if(!host)return;const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(48,1,.1,100);camera.position.z=8.5;const renderer=new THREE.WebGLRenderer({antialias:false,alpha:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(window.devicePixelRatio||1,1.5));host.appendChild(renderer.domElement);
const count=window.innerWidth<700?14000:30000;const positions=new Float32Array(count*3);const seeds=new Float32Array(count*4);const TAU=Math.PI*2;
for(let n=0;n<count;n++){const band=n%6;const lane=Math.floor(n/6);const u=lane/Math.max(1,count/6-1);const phase=u*TAU*5.5+band*TAU/6;const radius=1.0+3.5*Math.pow(u,.72)+.18*Math.sin(phase*3+band);const x=Math.cos(phase)*radius*1.08;const y=Math.sin(phase)*radius*.72;const z=Math.sin(phase*1.7+band)*.72+(u-.5)*1.3;const k=n*3;positions[k]=x+1.0;positions[k+1]=y;positions[k+2]=z;const q=n*4;seeds[q]=(n%997)/997;seeds[q+1]=(n%991)/991;seeds[q+2]=(n%983)/983;seeds[q+3]=band/6;}
const geometry=new THREE.BufferGeometry();geometry.setAttribute('position',new THREE.BufferAttribute(positions,3));geometry.setAttribute('aSeed',new THREE.BufferAttribute(seeds,4));
const material=new THREE.ShaderMaterial({transparent:true,depthWrite:false,blending:THREE.AdditiveBlending,uniforms:{uTime:{value:0},uMouse:{value:new THREE.Vector2()},uAccent:{value:new THREE.Color('#a96832')},uLight:{value:new THREE.Color('#edc078')}},vertexShader:`attribute vec4 aSeed;uniform float uTime;uniform vec2 uMouse;varying float vGlow;varying float vBand;void main(){vec3 p=position;float t=uTime*.42;float base=atan(p.y,p.x-1.0);float r=length(p.xy-vec2(1.0,0.0));float flow=sin(base*3.0-r*1.35-t*2.0+aSeed.x*6.2831)*.10;float twist=t*.16+r*.055+aSeed.w*6.2831;p.xy+=vec2(cos(twist),sin(twist))*flow;p.z+=sin(base*2.0+r*1.4-t*2.2+aSeed.y*6.2831)*.12;vec2 m=uMouse*4.8;vec2 d=p.xy-m;float md=length(d);float influence=exp(-md*1.05);vec2 tangent=vec2(-d.y,d.x);p.xy+=normalize(tangent+vec2(.0001))*influence*.34;p.xy+=normalize(d+vec2(.0001))*influence*.28;float pulse=.72+.28*sin(t*2.0+aSeed.z*6.2831);vGlow=pulse*(1.0-smoothstep(1.0,5.3,r));vBand=aSeed.w;vec4 mv=modelViewMatrix*vec4(p,1.0);gl_Position=projectionMatrix*mv;gl_PointSize=(1.15+2.0*aSeed.x)*pulse*(74.0/-mv.z);}`,fragmentShader:`uniform vec3 uAccent;uniform vec3 uLight;varying float vGlow;varying float vBand;void main(){vec2 uv=gl_PointCoord-.5;float d=length(uv);float core=smoothstep(.22,0.,d);float halo=smoothstep(.5,.08,d);float a=(core*.82+halo*.22)*vGlow;vec3 c=mix(uAccent,uLight,core*.72+vBand*.12);gl_FragColor=vec4(c,a*.72);}`});
const points=new THREE.Points(geometry,material);scene.add(points);const target=new THREE.Vector2(),mouse=new THREE.Vector2();const onMove=e=>{target.x=(e.clientX/window.innerWidth-.5)*2;target.y=-(e.clientY/window.innerHeight-.5)*2};const resize=()=>{const w=host.clientWidth||window.innerWidth,h=host.clientHeight||window.innerHeight;camera.aspect=w/h;camera.updateProjectionMatrix();renderer.setSize(w,h,false)};window.addEventListener('pointermove',onMove,{passive:true});window.addEventListener('resize',resize);resize();let frame=0;const clock=new THREE.Clock();const animate=()=>{frame=requestAnimationFrame(animate);const t=clock.getElapsedTime();mouse.lerp(target,.045);material.uniforms.uTime.value=t;material.uniforms.uMouse.value.copy(mouse);points.rotation.z=Math.sin(t*.055)*.16;points.rotation.y=Math.sin(t*.035)*.12;renderer.render(scene,camera)};animate();return()=>{cancelAnimationFrame(frame);window.removeEventListener('pointermove',onMove);window.removeEventListener('resize',resize);geometry.dispose();material.dispose();renderer.dispose();if(renderer.domElement.parentNode===host)host.removeChild(renderer.domElement)}},[]);return <div ref={ref} className="particle-bg" aria-hidden="true"/>}

export default function App(){const[now,setNow]=useState(new Date()),[manual,setManual]=useState(null),[selected,setSelected]=useState(null),[menu,setMenu]=useState(false);useEffect(()=>{const t=setInterval(()=>setNow(new Date()),60000);return()=>clearInterval(t)},[]);const auto=useMemo(()=>phase(now.getHours()),[now]),theme=manual||auto;useEffect(()=>{document.documentElement.dataset.theme=theme},[theme]);const go=id=>{document.getElementById(id)?.scrollIntoView({behavior:'smooth'});setMenu(false)};return <div className="site"><ParticleBackground/><div className="glass-vignette"/><header className="nav glass"><button className="brand" onClick={()=>go('top')}><b>SM</b><span>SWEEKAR M.</span></button><nav className={menu?'links open':'links'}>{['work','about','stack','contact'].map(x=><button key={x} onClick={()=>go(x)}>{x}</button>)}</nav><div className="navtools"><span className="time"><i/> {manual?manual==='day'?'LIGHT':'DARK':auto==='golden'?'GOLDEN HOUR':auto.toUpperCase()}</span><button className="circle" onClick={()=>setManual(manual==='day'?'night':'day')}>{manual==='day'?<Moon size={16}/>:<Sun size={16}/>}</button><button className="circle menu" onClick={()=>setMenu(!menu)}>{menu?<X size={16}/>:<Sparkles size={16}/>}</button></div></header><main id="top"><section className="hero wrap"><div className="glass hero-card"><p className="eyebrow"><span/> PORTFOLIO · AI SOFTWARE ENGINEER</p><p className="name-kicker">THIS IS</p><motion.h1 className="name-title" initial={{opacity:0,y:30,filter:'blur(10px)'}} animate={{opacity:1,y:0,filter:'blur(0)'}} transition={{duration:1}}>SWEEKAR<br/><span>M.</span></motion.h1><h2 className="hero-role">I build software with a <em>brain.</em></h2><p className="lead">AI systems, developer tools and production backends — built from first principles and shipped for real.</p><div className="buttons"><button className="primary" onClick={()=>go('work')}>Explore my work <ArrowUpRight size={18}/></button><a className="secondary" href="mailto:sweekar.m.work@gmail.com">Talk to me <Mail size={17}/></a></div><div className="numbers"><span><b>200+</b> AI actions</span><span><b>10+</b> backend modules</span><span><b>8.0</b> BCA CGPA</span></div></div></section><section className="identity-strip glass"><div><small>NAME</small><b>SWEEKAR M.</b></div><div><small>BASED IN</small><b>BENGALURU, INDIA</b></div><div><small>FOCUS</small><b>AI · SYSTEMS · PRODUCT</b></div></section><section className="ticker wrap"><div className="glass"><Rocket size={15}/><span><small>NOW BUILDING</small><b>AI-native developer tooling</b></span></div><div className="glass"><Sparkles size={15}/><span><small>INTERESTED IN</small><b>Agents · Retrieval · Systems</b></span></div><div className="glass"><Terminal size={15}/><span><small>PHILOSOPHY</small><b>Ship useful things.</b></span></div></section><section id="work" className="wrap section"><div className="heading glass"><small>01</small><div><h2>Things I’ve built.</h2><p>Systems designed around real technical problems.</p></div></div><div className="projects">{projects.map(([n,t,k,d,tags],i)=><motion.button key={t} className="project glass" initial={{opacity:0,y:35}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.15}} transition={{delay:i*.08}} whileHover={{y:-9,scale:1.01}} onClick={()=>setSelected({t,k,d,tags})}><span className="num">{n}</span><div className="projectshape"/><small>{k}</small><h3>{t}</h3><p>{d}</p><div className="tags">{tags.map(x=><span key={x}>{x}</span>)}</div></motion.button>)}</div></section><section id="about" className="wrap section"><div className="about glass"><div className="heading"><small>02</small><div><h2>Engineer first.<br/>AI everywhere.</h2></div></div><p className="bigcopy">I work at the intersection of applied AI and serious software engineering — retrieval, model routing, APIs, auth, observability and modular architecture.</p><div className="experience"><div><small>2026 → NOW</small><b>Backend Engineer Intern</b><span>CyberARC MSP / CarcMail · Python · FastAPI · Supabase · JWT</span></div><div><small>2022 → 2025</small><b>Bachelor of Computer Applications</b><span>SDM College, Ujire · CGPA 8.0</span></div></div></div></section><section id="stack" className="wrap section"><div className="heading glass"><small>03</small><div><h2>My toolbox.</h2><p>The technologies I reach for when something needs to actually work.</p></div></div><div className="skillcloud">{skills.map((x,i)=><motion.span className="glass" key={x} whileHover={{scale:1.08,rotate:i%2?2:-2,y:-3}}><Code2 size={14}/>{x}</motion.span>)}</div></section><section id="contact" className="wrap contact"><div className="contactinner glass"><div><small>04 · OPEN TO BUILDING</small><h2>Have a weird problem?<br/><em>Good.</em></h2><p>Let’s make it a system.</p></div><div className="contactbuttons"><a className="primary invert" href="mailto:sweekar.m.work@gmail.com">Email me <Mail size={18}/></a><a className="circle dark" href="https://github.com/Sweekar-m" target="_blank" rel="noreferrer"><Github size={18}/></a><a className="circle dark" href="https://www.linkedin.com/" target="_blank" rel="noreferrer"><Linkedin size={18}/></a></div></div><footer>© {now.getFullYear()} Sweekar M. <span>React · Three.js · curiosity</span></footer></section></main><AnimatePresence>{selected&&<motion.div className="modalbg" initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} onClick={()=>setSelected(null)}><motion.div className="modal glass" initial={{y:30,scale:.96}} animate={{y:0,scale:1}} exit={{y:30,scale:.96}} onClick={e=>e.stopPropagation()}><button className="close circle" onClick={()=>setSelected(null)}><X size={17}/></button><small>{selected.k}</small><h2>{selected.t}</h2><p>{selected.d}</p><div className="tags">{selected.tags.map(x=><span key={x}>{x}</span>)}</div></motion.div></motion.div>}</AnimatePresence></div>}
